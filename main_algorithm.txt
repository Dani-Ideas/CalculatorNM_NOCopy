# sintetic division
# sintetic division
# sintetic division
# sintetic division

def ruffini(coefficients, root):
# Coeficientes de la ecuación
n = len(coefficients) - 1
result = [0] *n # Resultado de la división
result[0] = coefficients[0] # Primer coeficiente
# Proceso de Ruffini
for i in range(1, n):
result[i]= coefficients[i] + root result[i - 1]
return result, result[-1] # Devuelve el cociente y el residuo
# Entrada del usuario
Copy coc
input_str = input("Ingresa los coeficientes de la ecuación separados por comas (ej: 1,
coefficients = list(map(float, input_str.split(',')))
root = float(input("Ingresa la raíz (valor de x) que deseas usar: "))
# Llamada al método de Ruffini
quotient, remainder = ruffini (coefficients, root)
print("Cociente:", quotient)
print("Residuo:", remainder)


# Raphson Method
# Raphson Method
# Raphson Method
# Raphson Method

# An example function whose solution
# is determined using Bisection Method.
# The function is x^3 - x^2 + 2




# Bisection Method
#Bisection Method
#Bisection Method

# An example function whose
# solution is determined using
# Bisection Method.
# The function is x^3 - x^2  + 2





Como puedes ver te pase dos metodos numericos, estos consisten en el metodo de newton Rapson y el metodo de biseccion ambos cuentan con una funcion llamada "func" que la que se encagga de evaluar el valor en x:
Contexto:
1- yo desarrollare un programa que selecione que metodo numerico quiero usar, lo primero que me pide es que funcion se va a usar, para eso quiero esta eintefaz grafica:
"root = Tk()
root.title("PYTHON GUI SCIENTIFIC CALCULATOR")
root.geometry("600x625+50+40")
root.resizable(0, 0)"
2 - ¿Es posible usar mi intefas grafica solo para definir la funcion con "X"?, en ves de eniarlo a calc 
3 - Luego el programa solicitara el valor de todas la "X" requeridas
4 - Crear una funcion que sustitulla todas las "X" y agregando si aplica la multiplicacion implicita al string
5 - envia el strig a "def cal():" para que el resulado sea usado por el metodo numerico selecionado al principio del programa hasta allar la solucion del metod numerico